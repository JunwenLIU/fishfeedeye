# 2026-08-29 工作日志

## 项目：鱼类摄食行为视频分析工具（software-fish-feeding 团队）

### 调研阶段（已完成）
- PM（许清楚）产出 `docs/01-research-fish-feeding-behavior.md`（682行）：学术进展、诱食性评价范式、A/B/C/D 四组指标体系（可视频自动提取性分级）、拍摄与对照实验设计规范、FCR/SGR 等不可输出指标清单
- 架构师（高见远）产出 `docs/02-research-open-source-stack.md`（1821行，v14）：技术栈拍板（Python311+torch cu126+Ultralytics+Supervision+OpenCV+Gradio+pingouin）；GitHub 实查无可直接 fork 的鱼类摄食分析项目；COCO 无 fish 类，零训练不可行，需标注 30–80 帧微调；户外斜拍诚实精度评估（典型 ±10–20%，T50/T90 更稳健）；浮动投喂框四合一方案
- PM 产出 `docs/03-PRD.md`（512行，P0 42条）、`docs/04-metrics-input-contract.md`（17列冻结 schema）、`docs/05-user-questionnaire.md`
- PM 与架构师交叉核验 11 轮，形成六条设计原则（不得无法兑现的承诺、边界处不用有语义默认值、可比性决策版本化、数值与边界同传、列必须有消费方、禁止可信度摘要标量）

### 用户三轮决策（已固化到 docs/00-user-decisions.md）
浮性膨化料 / 户外池塘网箱 / 愿标注 / 科研进论文 / 两组对照 / 投喂框+参照物可布设 / 三脚架斜拍 / 要打点计数工具 / 每段有≥30s基线 / 三种实验结构都支持 / 有纯饲料标定素材 / 1080p·≥5min·≥10段

### 架构设计（已完成）
- `docs/06-architecture-design.md`（675行）：38 源文件 + 7 测试文件，5 任务（T01 契约层 → T02 接入预处理 ‖ T03 检测计数双轨 → T04 指标质量 → T05 UI统计导出）
- 最小闭环：T01→T02→T03→T04 + run_cli.py；T05 包 Gradio 六页签

### 环境搭建
- 本机实测：RTX 3060 Ti 8GB / CUDA 12.6 toolkit / 系统 Python 3.11.4（原装 CPU 版 torch 需替换）
- 项目 venv：`H:\workbuddy\鱼类摄食行为视频识别\.venv`（Python 3.11.4）
- torch+torchvision cu126 版后台安装中

### 进行中
- 工程师（寇豆码）实现 T01（基础设施+契约层：MetricValue/RunConfig/ROI/Homography/timing + 测试）

### 会话中断恢复（08:06）
- 凌晨 01:02 因 API 用量限制（429）中断：T01 代码已落盘但 torch 后台安装未完成、团队会话失效
- 恢复后验证：T01 测试 43/43 全部通过（src/core 5 模块 + pipeline/timing + utils + 2 测试文件均完好）
- 重建团队 software-fish-feeding-16e2，重派工程师实现 T02（接入+预处理+标定）+ T03（检测计数双轨），并重启 torch cu126 后台安装
- 注意：后续若再中断，恢复流程 = 验证已落盘代码测试 → 重建团队 → 续派下一任务

### T02/T03 完成 + T04 并发冲突（08:30–09:30）
- T02 交付：ingest.py（非对称采样/PTS时间源/基线≥30s门）、preprocess.py（稳像+单应性+CLAHE+参考区扣除）、pellet_dynamics.py（HSV分割/A_single标定）、scripts/00_calibrate_pellet_dynamics.py
- T03 交付：detectors/{base,yolo,yoloe,area_integral}.py（双轨+SAHI切片+NMS）、pellet_linker.py（失踪三分类）、orchestrator.py（断点续跑jsonl缓存/Q_dualtrack_gap）
- **171 测试全过**；GPU 冒烟通过：RTX 3060 Ti，YOLOE 权重+ mobileclip_blt.ts(572MB) 已下载，稳态 0.216s/帧，cuda_memory_allocated=91.3MB；合成圆点帧出框=0（开放词汇域差距，非 bug，待真实视频验证）
- 环境坑：ultralytics 8.4.132 自带顶层 `tests` 包，遮蔽项目 tests/ 导致 pytest 收集失败 → 解法：项目 tests/ 加 `__init__.py` 声明为正规包（**不许删除该文件**）
- 并发写入事故：误判工程师提前终止而加派了第二个工程师，两人同写 src/metrics 造成三次覆盖与两套 API 冲突（aggregator 42KB 版本引用不存在符号）
- **教训（后续强制执行）**：同一文件域只能有单一工程师；teammate 是消息驱动唤醒机制，TaskStop 后仍会被任何消息（含 peer DM）唤醒继续写文件 → 停止某 teammate 必须同时：发 shutdown_request + 禁止其他成员与其通信
- T04 代码+测试已落盘（src/metrics 17 模块、3 个 test 文件），但 API 未收口：__init__ 导入的 MetricsSummary/build_baseline_stats 在 aggregator 不存在，包 ImportError

### 会话中断恢复（08-30 01:45）
- 09:21 再次 429 额度耗尽，两端工程师同时失败
- 恢复：清理 77 个临时调试 txt/py → 归档到 `_scratch/`；重建团队 software-fish-feeding-2919，派新会话工程师：先收口 T04（统一 API 口径，以 docs/04+docs/06 为准，171 基线保持）→ 再 T05（Gradio/t检验+效应量/打点计数/导出）
